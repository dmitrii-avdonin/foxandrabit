//just install pipreqs
pip install pipreqs
//save all project dependecies in requirements.txt by the path
pipreqs --encoding=utf8 "D:\Projects\Tensorflow\foxandrabit"
//install everything from requirements.txt
pip install -r requirements.txt